name: Deploy GitHub Pages snapshot

on:
  push:
    branches: [main]
    paths-ignore:
      - 'docs/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Generate snapshot JSONs
        run: python3 snapshot.py

      - name: Copy static site into docs/
        run: |
          cp static/index.html docs/index.html
          cp static/app.js docs/app.js
          [ -f static/style.css ] && cp static/style.css docs/style.css
          rm -rf docs/snapshot
          cp -r snapshot docs/snapshot
          sed -i 's#<script src="/static/app.js"></script>#<script src="app.js"></script>#' docs/index.html
          sed -i 's#id="staticModeNotice" style="display:none;#id="staticModeNotice" style="display:block;#' docs/index.html

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/
          if git diff --cached --quiet; then
            echo "No changes to deploy"
          else
            git commit -m "Auto-update GitHub Pages snapshot [skip ci]"
            git push
          fi
